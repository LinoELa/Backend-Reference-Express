# @config

Esta carpeta guarda configuraciones compartidas del proyecto.

Ejemplo:

- `db.js`: configura Prisma, el adaptador `@prisma/adapter-pg` y la conexion a PostgreSQL

Aqui no suele ir logica de negocio. Solo configuracion que otros archivos necesitan reutilizar.
